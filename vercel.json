{
  "buildCommand": "npm run build",
  "framework": "angular",
  "installCommand": "npm install --force",
  "outputDirectory": "dist/project-test",
  "devCommand": "ng serve",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}